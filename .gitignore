# Ephemeral loop run state (snapshots, reviews, state.json) — never synced/tracked
.loop/

# Ephemeral spec-driven-development session artifacts (task briefs, review diffs) — never tracked
.superpowers/

# Node
node_modules/
npm-debug.log*

# OS
.DS_Store

bench/reports/

# SWE-EVO dataset cache (7.5MB, rebuilt from HF at the pinned SHA via `dataset.mjs --fetch`);
# only the small PINNED.json manifest is tracked.
bench/swe-evo/data/instances-*.json

# Tier-3 plan files — ephemeral per-session reasoning trace; design intent lives in docs/design/specs/ (tracked)
docs/design/plans/
