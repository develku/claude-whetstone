# Ephemeral loop run state (snapshots, reviews, state.json) — never synced/tracked
.loop/

# Node
node_modules/
npm-debug.log*

# OS
.DS_Store

bench/reports/

# Tier-3 plan files — ephemeral per-session reasoning trace; design intent lives in docs/superpowers/specs/ (tracked)
docs/superpowers/plans/
